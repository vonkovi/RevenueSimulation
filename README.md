# RevenueSimulation

RUN IN TERMINAL
1. `docker compose up --build`
2. `docker compose run --rm --service-ports simulation "agentsociety ui"`

UPDATE CONFIG.YAML
1. go to platform.openai.com
2. get your api key
3. replace placeholder value in .env file